---
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import Layout from "../layouts/Layout.astro";
---

<Layout
  title={SITE_TITLE}
  description={SITE_DESCRIPTION}
  className="flex min-h-screen flex-col items-center justify-center bg-gray-100 dark:bg-gray-900"
>
  <div class="w-full max-w-2xl p-4">
    <!-- Bento Grid Container -->
    <div class="grid grid-cols-3 gap-3">
      <!-- 메인 타이틀 카드 (전체 너비) -->
      <div
        class="col-span-3 rounded-2xl border border-gray-200 bg-white p-6 shadow-sm dark:border-gray-700 dark:bg-gray-800"
      >
        <h1 class="mb-3 text-2xl font-bold text-gray-900 dark:text-gray-100">
          나쁜 일은 한 순간에 일어나고,<br />좋은 일은 오래 걸린다
        </h1>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          기술은 빠르게 변하지만, 좋은 코드를 만드는 원칙은 변하지 않습니다.
          이곳은 변하는 기술 속에서 변하지 않는 개발의 지혜를 기록하는 공간입니다.
        </p>
      </div>

      <!-- 기술 스택 카드 (2/3 너비) -->
      <div
        class="col-span-2 rounded-2xl border border-gray-200 bg-white p-4 shadow-sm dark:border-gray-700 dark:bg-gray-800"
      >
        <h2 class="mb-2 text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400">
          Tech Stack
        </h2>
        <div class="flex flex-wrap gap-1">
          <span class="rounded-md bg-blue-100 px-2 py-1 text-xs font-medium text-blue-800 dark:bg-blue-900 dark:text-blue-200">
            TypeScript
          </span>
          <span class="rounded-md bg-green-100 px-2 py-1 text-xs font-medium text-green-800 dark:bg-green-900 dark:text-green-200">
            React
          </span>
          <span class="rounded-md bg-purple-100 px-2 py-1 text-xs font-medium text-purple-800 dark:bg-purple-900 dark:text-purple-200">
            Node.js
          </span>
        </div>
      </div>

      <!-- 블로그 포스트 링크 (1/3 너비) -->
      <a
        href="/posts"
        class="col-span-1 flex items-center justify-center rounded-2xl border border-gray-200 bg-gray-900 p-4 text-white shadow-sm transition-all hover:scale-105 hover:bg-gray-800 dark:border-gray-700 dark:bg-black dark:hover:bg-gray-900"
      >
        <span class="text-sm font-medium">포스트 읽기</span>
      </a>
    </div>
  </div>
</Layout>