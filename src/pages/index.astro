---
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import Layout from "../layouts/Layout.astro";
---

<Layout
  title={SITE_TITLE}
  description={SITE_DESCRIPTION}
  className="flex min-h-screen flex-col items-center justify-center bg-gray-50 dark:bg-gray-900"
>
  <style>
    .grid-pattern {
      background-image: repeating-linear-gradient(
        45deg,
        transparent,
        transparent 10px,
        rgba(0, 0, 0, 0.03) 10px,
        rgba(0, 0, 0, 0.03) 20px
      );
    }
    .dark .grid-pattern {
      background-image: repeating-linear-gradient(
        45deg,
        transparent,
        transparent 10px,
        rgba(255, 255, 255, 0.03) 10px,
        rgba(255, 255, 255, 0.03) 20px
      );
    }
  </style>

  <div class="grid-pattern w-full max-w-2xl rounded-lg p-8">
    <!-- Bento Grid Container with Grid Lines -->
    <div class="grid grid-cols-3 border-l border-t border-gray-300 dark:border-gray-700">
      <!-- 메인 타이틀 카드 (전체 너비) -->
      <div
        class="col-span-3 border-b border-r border-gray-300 bg-white/80 p-6 backdrop-blur-sm dark:border-gray-700 dark:bg-gray-800/80"
      >
        <h1 class="mb-3 text-2xl font-bold text-gray-900 dark:text-gray-100">
          나쁜 일은 한 순간에 일어나고,<br />좋은 일은 오래 걸린다
        </h1>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          기술은 빠르게 변하지만, 좋은 코드를 만드는 원칙은 변하지 않습니다.
          이곳은 변하는 기술 속에서 변하지 않는 개발의 지혜를 기록하는
          공간입니다.
        </p>
      </div>

      <!-- 기술 스택 카드 (2/3 너비) -->
      <div
        class="col-span-2 border-b border-r border-gray-300 bg-white/80 p-6 backdrop-blur-sm dark:border-gray-700 dark:bg-gray-800/80"
      >
        <h2
          class="mb-3 text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400"
        >
          Tech Stack
        </h2>
        <div class="flex flex-wrap gap-2">
          <span
            class="border border-gray-300 bg-gray-50 px-3 py-1 text-xs font-medium text-gray-700 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-300"
          >
            TypeScript
          </span>
          <span
            class="border border-gray-300 bg-gray-50 px-3 py-1 text-xs font-medium text-gray-700 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-300"
          >
            React
          </span>
          <span
            class="border border-gray-300 bg-gray-50 px-3 py-1 text-xs font-medium text-gray-700 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-300"
          >
            Node.js
          </span>
        </div>
      </div>

      <!-- 블로그 포스트 링크 (1/3 너비) -->
      <a
        href="/posts"
        class="col-span-1 flex items-center justify-center border-b border-r border-gray-300 bg-gray-900/90 p-6 text-white transition-all hover:bg-gray-800 dark:border-gray-700 dark:bg-black/90 dark:hover:bg-gray-900"
      >
        <span class="text-sm font-medium">포스트 읽기</span>
      </a>
    </div>
  </div>
</Layout>