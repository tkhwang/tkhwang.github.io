---
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import Layout from "../layouts/Layout.astro";
---

<style>
  .grid-background {
    background-image:
      linear-gradient(rgba(0, 0, 0, 0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0, 0, 0, 0.02) 1px, transparent 1px);
    background-size: 50px 50px;
  }
  .dark .grid-background {
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
    background-size: 50px 50px;
  }
</style>

<Layout
  title={SITE_TITLE}
  description={SITE_DESCRIPTION}
  className="grid-background flex min-h-screen flex-col items-center justify-center bg-gray-50 dark:bg-gray-900"
>
  <div class="w-full max-w-3xl p-8">
    <!-- Bento Grid Container with Grid Lines -->
    <div
      class="grid grid-cols-3 border-l border-t border-gray-200 dark:border-gray-700"
    >
      <!-- 메인 타이틀 카드 (전체 너비) -->
      <div
        class="col-span-3 border-b border-r border-gray-200 bg-white/90 p-6 backdrop-blur-sm dark:border-gray-700 dark:bg-gray-800/90"
      >
        <h1 class="mb-3 text-2xl font-bold text-gray-900 dark:text-gray-100">
          나쁜 일은 한 순간에 일어나고, 좋은 일은 오래 걸린다
        </h1>
        <p class="text-base text-gray-600 dark:text-gray-400">
          기술은 빠르게 변하지만, 좋은 코드를 만드는 원칙은 변하지 않습니다.
          변하는 기술 속에서 변하지 않는 개발의 지혜를 찾아가는 과정을
          기록합니다.
        </p>
      </div>

      <!-- 기술 스택 카드 (2/3 너비) -->
      <div
        class="col-span-2 border-b border-r border-gray-200 bg-white/90 p-6 backdrop-blur-sm dark:border-gray-700 dark:bg-gray-800/90"
      >
        <h2
          class="mb-3 text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400"
        >
          Tech Stack
        </h2>
        <div class="flex flex-wrap gap-2">
          <span
            class="inline-flex items-center rounded-full border border-transparent bg-blue-100 px-2.5 py-0.5 text-xs font-semibold text-blue-900 transition-colors hover:bg-blue-200 dark:bg-blue-900 dark:text-blue-100 dark:hover:bg-blue-800"
          >
            TypeScript
          </span>
          <span
            class="inline-flex items-center rounded-full border border-transparent bg-red-100 px-2.5 py-0.5 text-xs font-semibold text-red-900 transition-colors hover:bg-red-200 dark:bg-red-900 dark:text-red-100 dark:hover:bg-red-800"
          >
            Nest.js
          </span>
          <span
            class="inline-flex items-center rounded-full border border-transparent bg-cyan-100 px-2.5 py-0.5 text-xs font-semibold text-cyan-900 transition-colors hover:bg-cyan-200 dark:bg-cyan-900 dark:text-cyan-100 dark:hover:bg-cyan-800"
          >
            React.js
          </span>
          <span
            class="inline-flex items-center rounded-full border border-transparent bg-green-100 px-2.5 py-0.5 text-xs font-semibold text-green-900 transition-colors hover:bg-green-200 dark:bg-green-900 dark:text-green-100 dark:hover:bg-green-800"
          >
            Node.js
          </span>
          <span
            class="inline-flex items-center rounded-full border border-transparent bg-purple-100 px-2.5 py-0.5 text-xs font-semibold text-purple-900 transition-colors hover:bg-purple-200 dark:bg-purple-900 dark:text-purple-100 dark:hover:bg-purple-800"
          >
            AI
          </span>
        </div>
      </div>

      <!-- 블로그 포스트 링크 (1/3 너비) -->
      <a
        href="/posts"
        class="col-span-1 flex items-center justify-center border-b border-r border-gray-200 bg-gray-900/90 p-6 text-white transition-all hover:bg-gray-800 dark:border-gray-700 dark:bg-black/90 dark:hover:bg-gray-900"
      >
        <span class="text-base font-medium sm:whitespace-nowrap">
          <span class="block text-center sm:hidden">포스트<br />읽 기</span>
          <span class="hidden sm:inline">포스트 읽기</span>
        </span>
      </a>
    </div>
  </div>
</Layout>
